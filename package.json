{
  "name": "carnatic-gpt",
  "version": "1.0.0",
  "type": "module",
  "description": "CarnaticGPT AI-powered RAG assistant",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "python3 -m uvicorn backend.server:app --reload",
    "install:all": "cd frontend && npm install && pip install -r requirements.txt"
  }
}
